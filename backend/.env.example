# Server
PORT=4000

# JWT Secret (change in production!)
JWT_SECRET=your-super-secret-jwt-key-change-this

# Anthropic API Key for AI features
ANTHROPIC_API_KEY=your-anthropic-api-key

# LinkedIn OAuth (get from LinkedIn Developer Portal)
LINKEDIN_CLIENT_ID=your-linkedin-client-id
LINKEDIN_CLIENT_SECRET=your-linkedin-client-secret
LINKEDIN_REDIRECT_URI=http://localhost:4000/api/linkedin/callback

# Database Configuration (REQUIRED - choose one option below)
# ================================================================

# Option 1: PostgreSQL (recommended for production - Railway, Render, etc.)
# DATABASE_URL=postgresql://user:password@host:5432/database_name

# Option 2: Railway PostgreSQL (auto-injected when deployed)
# DATABASE_URL will be automatically set by Railway when you provision a database

# Example Railway format:
# DATABASE_URL=postgresql://postgres:xxx@containers-xxx.railway.app:5432/railway
